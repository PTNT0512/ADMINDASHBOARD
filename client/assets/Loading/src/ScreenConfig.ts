export default class ScreenConfig {
    // Portrait profile used by runtime layout code.
    static readonly DESIGN_WIDTH = 720;
    static readonly DESIGN_HEIGHT = 1280;
    static readonly FIT_WIDTH = true;
    static readonly FIT_HEIGHT = false;

    // Desktop build window size (builder.json uses these values).
    static readonly BUILD_WINDOW_WIDTH = 720;
    static readonly BUILD_WINDOW_HEIGHT = 1280;
}

